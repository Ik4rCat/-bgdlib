<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:bgdlib.ViewModels"
             xmlns:models="clr-namespace:bgdlib.Models"
             xmlns:conv="clr-namespace:bgdlib.Converters"
             x:Class="bgdlib.Views.NotesPage"
             x:DataType="vm:NotesViewModel"
             x:Name="NotesRoot"
             BackgroundColor="#0f0f0f"
             Shell.NavBarIsVisible="False">

    <ContentPage.Resources>
        <conv:InvertBoolConverter x:Key="InvertBool"/>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,Auto,1,*,Auto">

        <!-- AppBar -->
        <Grid Grid.Row="0" ColumnDefinitions="*,Auto"
              BackgroundColor="#1a1a1a" Padding="16,13,12,13">
            <Label Grid.Column="0" Text="Заметки" Style="{StaticResource AppBarTitle}"/>
            <!-- New note -->
            <Label Grid.Column="1" Text="&#xe145;" FontFamily="MaterialIcons"
                   FontSize="28" TextColor="#E53935" VerticalOptions="Center">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding NewNoteCommand}"/>
                </Label.GestureRecognizers>
            </Label>
        </Grid>

        <!-- Search -->
        <Border Grid.Row="1" BackgroundColor="#1a1a1a" StrokeThickness="0"
                Padding="12,8">
            <Border BackgroundColor="#242424" StrokeThickness="1" Stroke="#2a2a2a"
                    StrokeShape="RoundRectangle 10" Padding="0">
                <Grid ColumnDefinitions="Auto,*" Padding="12,0">
                    <Label Grid.Column="0" Text="&#xe8b6;" FontFamily="MaterialIcons"
                           FontSize="20" TextColor="#555555" VerticalOptions="Center"
                           Margin="0,0,8,0"/>
                    <Entry Grid.Column="1" Text="{Binding SearchQuery}"
                           Placeholder="Поиск по заметкам…"
                           PlaceholderColor="#555555" TextColor="#F5F5F5"
                           BackgroundColor="Transparent" FontSize="14"/>
                </Grid>
            </Border>
        </Border>

        <BoxView Grid.Row="2" BackgroundColor="#2a2a2a"/>

        <!-- Notes list / empty -->
        <Grid Grid.Row="3">
            <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center"
                                 Spacing="14" IsVisible="{Binding IsEmpty}">
                <Label Text="&#xe873;" FontFamily="MaterialIcons" FontSize="64"
                       TextColor="#2a2a2a" HorizontalOptions="Center"/>
                <Label Text="Нет заметок" TextColor="#888888"
                       FontSize="15" HorizontalOptions="Center"/>
                <Label Text="Нажми + чтобы создать первую" TextColor="#555555"
                       FontSize="13" HorizontalOptions="Center"/>
            </VerticalStackLayout>

            <CollectionView ItemsSource="{Binding Notes}" SelectionMode="None"
                            IsVisible="{Binding IsEmpty, Converter={StaticResource InvertBool}}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Note">
                        <VerticalStackLayout>
                            <Grid ColumnDefinitions="*,Auto" Padding="16,14">
                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={x:Reference NotesRoot}, Path=BindingContext.OpenNoteCommand}"
                                        CommandParameter="{Binding .}"/>
                                </Grid.GestureRecognizers>
                                <VerticalStackLayout Grid.Column="0" Spacing="5">
                                    <Label Text="{Binding Title}"
                                           FontFamily="OpenSansSemibold" FontSize="15"
                                           TextColor="#F5F5F5"/>
                                    <Label Text="{Binding Tags}" FontSize="11"
                                           TextColor="#E53935"
                                           IsVisible="{Binding Tags, Converter={StaticResource NotEmpty}}"/>
                                    <Label Text="{Binding UpdatedAt, StringFormat='{0:dd MMM yyyy}'}"
                                           FontSize="11" TextColor="#555555"/>
                                </VerticalStackLayout>
                                <!-- Delete swipe alternative: long-press menu -->
                                <Label Grid.Column="1" Text="&#xe872;" FontFamily="MaterialIcons"
                                       FontSize="20" TextColor="#3a3a3a" VerticalOptions="Center">
                                    <Label.GestureRecognizers>
                                        <TapGestureRecognizer
                                            Command="{Binding Source={x:Reference NotesRoot}, Path=BindingContext.DeleteCommand}"
                                            CommandParameter="{Binding .}"/>
                                    </Label.GestureRecognizers>
                                </Label>
                            </Grid>
                            <BoxView HeightRequest="1" BackgroundColor="#1a1a1a"/>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>

    </Grid>
</ContentPage>
