<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:bgdlib.Models"
             x:Class="bgdlib.Views.NotesPage"
             BackgroundColor="{StaticResource Bg}">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="&#xe145;" Clicked="OnNewNoteClicked" Order="Primary"/>
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto,*">

        <Border Grid.Row="0" Style="{StaticResource InputBorder}"
                Margin="16,8,16,4">
            <Grid ColumnDefinitions="Auto,*" Padding="12,0">
                <Label Grid.Column="0" Text="&#xe8b6;" FontFamily="MaterialIcons"
                       FontSize="20" TextColor="{StaticResource TextMuted}" VerticalOptions="Center"/>
                <Entry Grid.Column="1" Text="{Binding SearchQuery}"
                       Placeholder="{Binding [Notes_Search], Source={StaticResource Loc}}"
                       FontSize="14" Margin="10,0,0,0"/>
            </Grid>
        </Border>

        <CollectionView Grid.Row="1"
                        ItemsSource="{Binding Notes}"
                        SelectionMode="Single"
                        SelectionChanged="OnNoteSelected">
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="12">
                    <Label Text="&#xe88e;" FontFamily="MaterialIcons"
                           FontSize="48" TextColor="{StaticResource TextDisabled}" HorizontalOptions="Center"/>
                    <Label Text="{Binding [Notes_Empty], Source={StaticResource Loc}}"
                           TextColor="{StaticResource TextMuted}" HorizontalTextAlignment="Center"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>
            <CollectionView.Footer>
                <BoxView HeightRequest="12" Color="Transparent"/>
            </CollectionView.Footer>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:Note">
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems>
                                <SwipeItem Text="{Binding [Swipe_Delete], Source={StaticResource Loc}}"
                                           BackgroundColor="{StaticResource Accent}"
                                           Command="{Binding Source={RelativeSource AncestorType={x:Type ContentPage}}, Path=BindingContext.DeleteCommand}"
                                           CommandParameter="{Binding .}"/>
                            </SwipeItems>
                        </SwipeView.RightItems>
                        <Border Margin="16,4" Padding="14"
                                BackgroundColor="{StaticResource Surface}"
                                StrokeShape="RoundRectangle 12"
                                Stroke="{StaticResource Border}"
                                StrokeThickness="1">
                            <Grid RowDefinitions="Auto,Auto" RowSpacing="6">
                                <Label Text="{Binding Title}" TextColor="{StaticResource TextPrimary}"
                                       FontSize="15" FontAttributes="Bold"/>
                                <HorizontalStackLayout Grid.Row="1" Spacing="8">
                                    <Label Text="{Binding Tags}" TextColor="{StaticResource Accent}" FontSize="11"/>
                                    <Label Text="·" TextColor="{StaticResource TextDisabled}" FontSize="11"/>
                                    <Label Text="{Binding UpdatedAt, StringFormat='{0:dd.MM.yy}'}"
                                           TextColor="{StaticResource TextMuted}" FontSize="11"/>
                                </HorizontalStackLayout>
                            </Grid>
                        </Border>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
</ContentPage>
